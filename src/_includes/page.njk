<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="shortcut icon" href="favicon.ico">


    {# <link rel="icon" type="image/png" sizes="16x16" href="/images/favicon-16x16.png">
    <link rel="apple-touch-icon" sizes="180x180" href="/images/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/images/favicon-32x32.png">
    <meta name="description" content="{{ site.description }}" />
    <meta property="og:description" content="{{ site.og_description }}" />
    <meta name="keywords" content="{{ site.keywords }}" />
    <meta name="twitter:dnt" content="on" />
    <meta property="og:title" content="{{ site.og_title }}" />
    <meta property="og:site_name" content="{{ site.og_site_name }}" />
    <meta property="og:url" content="{{ site.og_url }}" />
    <meta property="og:type" content="{{ site.og_type }}" />
    <meta property="og:image" content="{{ site.og_image }}" />
    <meta name="twitter:site" content="{{ site.twitter_site }}" />
    <link rel="canonical" href="{{ site.canonical }}" />#}


    <link rel="stylesheet" href="/assets/main.bundle.css?version={{ version }}">
    <script defer src="/assets/main.bundle.js?version={{ version }}"></script>
    <title>{{ title | dump }}</title>
    
    
     {# Allows any pages header to be extended.   #}
    {# {% block head %}{% endblock %} #}
  </head>
  <body class="text-lg font-graphix-regular {{ 'debug-screens' if devMode }}">

    {% include 'header.njk' %}
    
    <main class="container mx-auto px-4">
    <h1 class="text-4xl my-4">The page title is : {{title }}</h1>
      {% block body %}{% endblock %}
    </main>
    
    {% include 'footer.njk' %} 
  </body>
</html>